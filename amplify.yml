version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install
        - node --version
        - npm --version
        - npm ci --cache .npm --prefer-offline
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: www
    files:
      - '**/*'
  cache:
    paths:
      - .npm/**/*
